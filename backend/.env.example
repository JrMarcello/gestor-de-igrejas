# Exemplo de arquivo .env para o backend

# URL de conexão do seu banco de dados MongoDB
# Substitua <db_username>, <db_password>, <host> e <database_name> pelos seus dados reais.
# Exemplo para MongoDB Atlas:
# DATABASE_URL="mongodb+srv://<db_username>:<db_password>@cluster0.atlfwpo.mongodb.net/<database_name>?retryWrites=true&w=majority&appName=Cluster0"
DATABASE_URL="mongodb+srv://<db_username>:<db_password>@<host>/<database_name>?retryWrites=true&w=majority&appName=Cluster0"

# Chave secreta para assinar os tokens JWT
# Gere uma string longa e aleatória para produção.
JWT_SECRET="SUA_CHAVE_SECRETA_JWT_AQUI"
