declare module '@prisma/client' {
  export enum ClassRole {
    TEACHER = 'TEACHER',
    STUDENT = 'STUDENT',
  }
  export enum AttendanceStatus {
    PRESENT = 'PRESENT',
    ABSENT = 'ABSENT',
  }
}
